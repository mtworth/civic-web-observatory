[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m uvicorn pwo.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
